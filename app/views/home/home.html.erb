<h1>Welcome to the home page of The Gossip Project !</h1></br>
<p>Want to know more about the team ? Click <a href="http://localhost:3000/team">here</a></p>
<p>Want to contact the team ? Click <a href="http://localhost:3000/contact">here</a> to get their info</p>

<h1>All Gossips are here!</h1>

    <% @gossip.each_with_index do | gossip , index| %>
      <p>
          Son auteur : **
        <%= gossip.user.first_name %>
        <%= %>
        **<br/>
        Le titre du potin : **
        <%= gossip.title %>
        **<br/>
      </p>
      <a href="/gossip/<%=index + 1 %>">Lien vers ce gossip</a><br/>
  </br>
    <% end %>